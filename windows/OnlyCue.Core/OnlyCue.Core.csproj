<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <ItemGroup>
    <!-- LtcDecoder's intermediate pipeline stages are internal: golden/ltc-decode-v1.json
         pins each one on its own, but they are not product API. This is the only
         internals grant in the solution. -->
    <InternalsVisibleTo Include="OnlyCue.Core.Tests" />
  </ItemGroup>

</Project>
